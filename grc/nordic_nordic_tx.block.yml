# auto-generated by grc.converter

id: nordic_nordic_tx
label: Nordic Transmitter
category: '[Nordic]'

parameters:
-   id: channel_count
    label: Number of Channels
    dtype: int
    default: '1'
    hide: part

inputs:
-   domain: message
    id: nordictap_in
    optional: true

outputs:
-   domain: stream
    dtype: byte
    multiplicity: ${ channel_count }

templates:
    imports: import nordic
    make: nordic.nordic_tx(${channel_count})

file_format: 1
